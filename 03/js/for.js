const students = ["Park", "Kim", "Lee", "Kang"];

for (let i = 0; i < students.length; i ++) {     // for (초깃값; 조건 ;증가식) {실행문}
  document.write(`${students[i]}.`);
}
// 순서: 초깃값 실행 -> 조건 체크 -> 실행 -> 카운터 증가